# Part-A development cell — per_step on K10 (rp_bdqn)
#
# Part-A switchboard cell per_step|on|K10 of the 10-cell structured partial factorial
# (freeze item 12). Development role: backbone numbers are the shared class-1
# placeholders and are superseded by the backbone-tuning pass (freeze item 2).
# Class-3 rule: ONLY the varied factor's own parameters may differ from the
# canonical cell — this file is audited by audits/c13_audit.py.
# C-COHERENCE at prior=on. prior_scale is the value SHARED by all prior=on cells.
run_id: cell_per_step_on_K10_deepsea_dev
role: development
part: A
method: rp_bdqn
env: deep_sea
master_seed: 0
use_rule: per_step
prior: 'on'
K: 10
arm: per_step|on|K10
backbone:
  lr: 0.0005
  batch_size: 32
  gamma: 0.99
ensemble_shared:
  mask_prob: 0.5
  head_loss_agg: grad_norm_1_over_k
factor_specific:
  prior_scale: 3.0
  eps_schedule: null
env_budget:
  deep_sea_size: 30
  episodes: 2000
seeds:
- 0
- 1
- 2
- 3
- 4
